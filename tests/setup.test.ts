import { describe, it, expect } from 'vitest'

describe('Project Setup', () => {
  it('test environment works correctly', () => {
    expect(true).toBe(true)
  })

  it('numeric calculations work correctly', () => {
    expect(1 + 1).toBe(2)
  })
}) 